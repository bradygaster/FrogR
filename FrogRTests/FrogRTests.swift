import XCTest
@testable import FrogR

final class FrogRTests: XCTestCase {
    func testAppLaunches() throws {
        // Placeholder — replace with real tests as game logic is added
        XCTAssertTrue(true, "FrogR test target is wired up")
    }
}
