# Xcode / xcodegen
*.xcodeproj/
*.xcuserdata
DerivedData/

# Build artifacts
.build/

# macOS
.DS_Store

# Secrets (real PAT lives here — never commit)
Secrets.xcconfig

# Squad: ignore runtime state (logs, inbox, sessions)
.squad/orchestration-log/
.squad/log/
.squad/decisions/inbox/
.squad/sessions/
.squad/.scratch/
# Squad: SubSquad activation file (local to this machine)
.squad-workstream
